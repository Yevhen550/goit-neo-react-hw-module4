import s from "./ImageGallery.module.css";

const ImageGallery = ({ images }) => {
  return (
    <div>
      {/* <ul>
        {images.map(({})=>(
        <li key={ }>
          <div>
            <img src={} alt={} />
          </div>
        </li>))}
      </ul> */}
    </div>
  );
};

export default ImageGallery;
